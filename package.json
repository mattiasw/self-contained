{
  "name": "self-contained",
  "version": "1.0.0",
  "description": "Self-contained web page data URI generator.",
  "scripts": {
    "build": "parcel build --public-url ./ index.html",
    "start": "parcel --https index.html",
    "watch": "parcel --https index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mattias Wallander",
  "license": "agpl-3.0",
  "devDependencies": {
    "parcel": "^2.0.1"
  },
  "dependencies": {
    "html-minifier-terser": "^7.0.0-alpha.0"
  }
}
